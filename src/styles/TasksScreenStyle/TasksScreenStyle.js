import { StyleSheet } from 'react-native';
import { SF, Fonts, SH, widthPercent, heightPercent, hexToRgba, Colors } from '../../utils';

// export default TasksScreenStyle = (Colors) => StyleSheet.create({
  export default StyleSheet.create({
   
});